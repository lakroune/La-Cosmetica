$kind: collection
name: images
description: >-
  ## Folder purpose (Cosmetica/images)


  This folder contains API requests related to managing product images in the
  Cosmetica backend—such as uploading/adding new images and any supporting image
  endpoints used by the application. It groups all image-handling operations in
  one place to keep media-related workflows organized and easy to find during
  development and testing.


  If you want, I can also:


  - add short descriptions to each request inside this folder (e.g., “add
  images”),
      
  - standardize naming (upload/get/delete) across image requests,
      
  - add basic tests at the folder level (status code + response time) so they
  run for every image request automatically.
auth:
  - id: 94ad1ad7-82c7-4386-90fb-087d98c00c3e
    type: bearer
    name: Bearer Token
    credentials:
      token: "{{tokenJWT}}"
order: 2517864334491647
